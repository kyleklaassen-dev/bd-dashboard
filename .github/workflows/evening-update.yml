# RETIRED 2026-05-21
#
# This workflow ran research.py at 02:30 UTC daily, duplicating the
# meridian-research.yml run at 06:00 UTC Mon–Sat. Both ran identical code;
# source_url dedup prevented double rows but both incurred Anthropic API costs
# for minimal incremental coverage (articles published in a 3.5-hour window).
#
# CONSOLIDATION: meridian-research.yml now runs daily at 06:00 UTC.
# This file is kept for reference but the schedule trigger is removed.
# The 02:30 UTC slot is reserved for future signal_monitor.py (Tier 1 — heuristic,
# no LLM, writes to signals table for targeted enrichment dispatch).
#
# To restore: re-add the schedule trigger below and uncomment the job body.

name: Evening Research Update (RETIRED)

on:
  workflow_dispatch:   # manual only — scheduled trigger removed

env:
  PYTHONPATH: ${{ github.workspace }}/src

jobs:
  evening-research:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Retired
        run: echo "This workflow is retired. research.py now runs daily via meridian-research.yml at 06:00 UTC."
